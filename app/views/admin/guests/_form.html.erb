<%= form_for([:admin, guest]) do |f| %>
  <% if guest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(guest.errors.count, "error") %> prohibited this guest from being saved:</h2>

      <ul>
      <% guest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_name %>
    <%= f.text_field :first_name %>
  </div>

  <div class="field">
    <%= f.label :last_name %>
    <%= f.text_field :last_name %>
  </div>

  <div class="field">
    <%= f.label :rsvp %>
    <%= f.check_box :rsvp %>
  </div>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="field">
    <%= f.label :num_assistants %>
    <%= f.number_field :num_assistants %>
  </div>

  <div class="field">
    <%= f.label :arrival_date %>
    <%= f.date_select :arrival_date %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
